# @adhd-companion/engine

Pure TypeScript mirror of orchestrator / monitor / privacy / day-boundary logic.
Vitest owns portable transitions; Rust in `../src-tauri` is residency source of truth.

Product Tauri app lives one level up. M0 Mac validation is parallel (contract v2.3).
