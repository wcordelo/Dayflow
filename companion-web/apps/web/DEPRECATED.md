# Deprecated browser surface

The files under `dist/` are retained only for migration review and rollback
analysis. Do not deploy or extend this bundle as a Dayflow companion app.

The supported product surfaces are the native Mac, Windows, Android/ChromeOS,
and iOS clients. Account/device sync belongs under
`companion-web/workers/api/src/` and must remain an opaque encrypted relay.
